import { OnInit, EventEmitter, TemplateRef, Injector } from '@angular/core';
import { TypeConverter } from '@farris/ui-property-panel';
import { AnonymousSet } from './anonymous-set.entity';
export declare class AnonymousSetComponent implements OnInit {
    private injector;
    cls: string;
    closeModal: EventEmitter<any>;
    submitModal: EventEmitter<any>;
    value: AnonymousSet;
    modalFooter: TemplateRef<any>;
    anonymousSet: AnonymousSet;
    modalConfig: {
        title: string;
        width: number;
        height: number;
        showButtons: boolean;
    };
    constructor(injector: Injector);
    ngOnInit(): void;
    clickOK(): void;
    clickCancel(): void;
}
export declare class AnonymousSetConverter implements TypeConverter {
    constructor();
    convertTo(data: any): string;
}
