import { OnInit, EventEmitter, TemplateRef, Injector } from '@angular/core';
import { DatagridComponent } from '@farris/ui-datagrid';
import { MessagerService } from '@farris/ui-messager';
import { ProcessDeUtil } from '../../domain/process-de-util';
import { EventDefinition } from './event-definition.entity';
import { TypeConverter } from '@farris/ui-property-panel';
export declare class EventDefinitionComponent implements OnInit {
    private message;
    private util;
    private injector;
    cls: string;
    closeModal: EventEmitter<any>;
    submitModal: EventEmitter<any>;
    value: any[];
    modalFooter: TemplateRef<any>;
    grid: DatagridComponent;
    modalConfig: {
        title: string;
        width: number;
        height: number;
        showButtons: boolean;
    };
    triggerActionCollection: any[];
    columns: any[];
    data: EventDefinition[];
    constructor(message: MessagerService, util: ProcessDeUtil, injector: Injector);
    ngOnInit(): void;
    add(): void;
    delete(): void;
    clickOK(): void;
    clickCancel(): void;
    private validator;
}
export declare class EventDefinitionConverter implements TypeConverter {
    constructor();
    convertTo(data: any[]): string;
}
