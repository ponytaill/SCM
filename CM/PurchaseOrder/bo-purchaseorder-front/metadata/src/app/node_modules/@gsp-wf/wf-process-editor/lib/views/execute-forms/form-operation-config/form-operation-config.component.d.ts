import { OnInit, TemplateRef, ComponentFactoryResolver, Injector } from '@angular/core';
import { FormMethodConfig } from '../entities/form-method-config';
import { HttpService } from '@ecp-caf/caf-common';
import { FormMethod } from '../entities/form-method';
import { DialogComponent } from '@farris/ui-dialog';
import { DatagridComponent } from '@farris/ui-datagrid';
import { MessagerService } from '@farris/ui-messager';
import { BsModalService } from '@farris/ui-modal';
export declare class FormOperationConfigComponent implements OnInit {
    private http;
    private message;
    private resolver;
    private injector;
    private modalService;
    private _fromFormatId;
    formFormatId: string;
    formFormatConfigId: string;
    operations: FormMethodConfig[];
    methodsDialog: DialogComponent;
    grid: DatagridComponent;
    methodGrid: DatagridComponent;
    cell: TemplateRef<any>;
    cols: any[];
    methodCols: {
        field: string;
        width: number;
        title: string;
        showTips: boolean;
    }[];
    e: any;
    formMethods: FormMethod[];
    state: 'add' | 'edit';
    constructor(http: HttpService, message: MessagerService, resolver: ComponentFactoryResolver, injector: Injector, modalService: BsModalService);
    ngOnInit(): void;
    add(): void;
    delete(): void;
    editParam(ctx: any): void;
    methodSelected(): void;
    validator(): boolean;
    private getFormMethods;
}
