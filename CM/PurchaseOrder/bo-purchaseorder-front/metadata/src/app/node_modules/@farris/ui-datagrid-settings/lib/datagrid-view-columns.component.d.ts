import { OnInit, Injector, EventEmitter, ChangeDetectorRef, OnChanges, SimpleChanges } from '@angular/core';
import { MultiSelectComponent } from '@farris/ui-multi-select';
export declare class DatagridViewColumnsComponent implements OnInit, OnChanges {
    private injector;
    private cd;
    idField: string;
    valueField: string;
    textField: string;
    private _selectedIds;
    selectedIds: any[];
    treeData: any[];
    ms: MultiSelectComponent;
    selectedIdsChange: EventEmitter<{}>;
    constructor(injector: Injector, cd: ChangeDetectorRef);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    onBeforeMove($event: any): void;
    hiddenColumn($event: MouseEvent, column: any): void;
}
