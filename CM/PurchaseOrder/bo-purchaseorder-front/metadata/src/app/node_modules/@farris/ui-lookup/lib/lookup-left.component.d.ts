import { OnInit, ViewContainerRef, EventEmitter, ComponentFactoryResolver, ComponentRef, ChangeDetectorRef } from '@angular/core';
import { Observable } from 'rxjs';
import { LookupGridOptions } from './lookup-grid-options';
import { LookupGridComponent } from './lookup-grid.component';
export declare class LookupLeftComponent implements OnInit {
    private cfr;
    private cd;
    cmpRef: ComponentRef<any>;
    navOptions: LookupGridOptions;
    lookupCmp: LookupGridComponent;
    cmpContainer: ViewContainerRef;
    selected: EventEmitter<{
        data: any;
    }>;
    unselected: EventEmitter<{}>;
    search: EventEmitter<{
        field: string;
        value: string;
    }>;
    pageChanged: EventEmitter<{
        pageInfo: {
            pageIndex: number;
            pageSize: number;
        };
        search: {
            field: string;
            value: string;
        };
    }>;
    private _searchState;
    private allData;
    constructor(cfr: ComponentFactoryResolver, cd: ChangeDetectorRef);
    ngOnInit(): void;
    createComponent(): Observable<any>;
    update(): void;
    resize(size?: {
        width: number;
        height: number;
    }): void;
    isTreeList(): boolean;
    private selectLeftDataTableRow;
    private closeLoading;
    private initDataTable;
    private initTreeTable;
    private onNodeExpanded;
    private getChildren;
    loadData(): void;
}
