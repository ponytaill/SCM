import { Observable, Subject } from "rxjs";
import { BatchScheme, DatagridSchemeModel, SchemeAuthModel, SchemeState } from "./setting.model";
export declare class DatagridSchemeService {
    private restService;
    private state;
    schemeList$: Subject<{}>;
    constructor();
    update(d: SchemeState, gridId: string): void;
    setRestService(httpSer: any): void;
    private getWebFormKey;
    getSchemeKey(gridId: string): string;
    getSchemeList(gridID: string): Observable<DatagridSchemeModel[]>;
    saveScheme(scheme: DatagridSchemeModel, gridID: string, isUpdate?: boolean): any;
    updateScheme(param: BatchScheme, gridID: string): Observable<any>;
    getStateValue(gridId: any, ...statePro: any[]): any;
    private updateStateValue;
    setSchemeList(gridId: any, newSchemeList: any): void;
    hasSchemeName(gridId: any, schemeName: any): boolean | any[];
    checkAuthority(): Observable<SchemeAuthModel>;
    getLanguages(): any;
}
