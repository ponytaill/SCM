import { Injector, ComponentFactoryResolver } from '@angular/core';
import { MessagerService } from '@farris/ui-messager';
import { BsModalService } from '@farris/ui-modal';
import { ForecastProcessPayload } from './entity/forecast-process-payload';
export declare class WFFlowchartService {
    private msgService;
    private injector;
    private resolver;
    private modalService;
    private flowchartService;
    private frameworkService;
    constructor(msgService: MessagerService, injector: Injector, resolver: ComponentFactoryResolver, modalService: BsModalService);
    viewProcess(payload: ForecastProcessPayload): void;
    /**
     * 查看流程（tab页中打开）
     * @param procInstId 流程实例ID
     */
    viewFlowChart(procInstId: string): void;
    viewFlowChartByDataId(dataId: string): void;
    /**
     * 查看流程（弹框中打开）
     * @param procInstId 流程实例ID
     */
    viewFlowChartByDialog(procInstId: string, mode?: string): void;
    private getI18nValue;
}
