export interface ButtonConfig {
    id: string;
    disable: boolean;
    title: string;
    click: any;
    appearance: object;
    visible?: boolean;
}
export interface ToolbarConfig {
    position: string;
    contents: ButtonConfig[];
}
