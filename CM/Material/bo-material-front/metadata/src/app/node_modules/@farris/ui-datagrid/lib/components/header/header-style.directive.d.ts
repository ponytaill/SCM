import { ElementRef, Renderer2, Injector, OnInit } from '@angular/core';
import { DataColumn } from './../../types/data-column';
export declare class HeaderStyleDirective implements OnInit {
    private el;
    private render;
    private inject;
    column: DataColumn;
    constructor(el: ElementRef, render: Renderer2, inject: Injector);
    ngOnInit(): void;
    private renderCss2Element;
    private renderStyle;
}
