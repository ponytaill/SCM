import { OnInit, TemplateRef } from '@angular/core';
import { ExpressionService } from '@gsp-svc/expression';
import { ProcessDesignerUIState } from '../../../app/process-designer.uistate';
import { Subject } from 'rxjs';
import { DatagridComponent } from '@farris/ui-datagrid';
export declare class ParameterSettingComponent implements OnInit {
    private expr;
    private designerState;
    modalFooter: TemplateRef<any>;
    grid: DatagridComponent;
    idField: 'Name' | 'code';
    private _mode;
    mode: 'bpmn' | 'usual';
    submit: Subject<any>;
    cancel: Subject<{}>;
    formTypeCollection: {
        text: string;
        value: string;
    }[];
    cols: any[];
    parameters: any[];
    formType: string;
    showFormType: boolean;
    constructor(expr: ExpressionService, designerState: ProcessDesignerUIState);
    ngOnInit(): void;
    showExprHelp(e: any): void;
    clickOK(): void;
    clickCancel(): void;
}
